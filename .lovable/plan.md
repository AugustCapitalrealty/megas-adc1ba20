

# Remover aviso de solicitação duplicada

Remover o bloco de verificação de duplicatas no `handleSubmit` de `src/pages/NovaSolicitacao.tsx` (linhas ~746-762) que exibe o `window.confirm` quando já existe uma solicitação aberta para o mesmo fornecedor + empreendimento.

**1 arquivo, remoção de ~16 linhas.**

